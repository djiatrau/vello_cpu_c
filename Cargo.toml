[package]
name = "vello_cpu_c"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
bytemuck = "1"
vello_cpu = { git = "https://github.com/linebender/vello", rev = "35a4279", features = ["multithreading"] }
